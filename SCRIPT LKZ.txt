local OrionLib = loadstring(game:HttpGet(('https://raw.githubusercontent.com/shlexware/Orion/main/source')))()

OrionLib:MakeNotification({
    Name = "Orion Example",
    Content = "Orion Example",
    Image = "rbxassetid://4483345998",
    Time = 5
})

local Window = OrionLib:MakeWindow({Name = "Orion Example", HidePremium = false, SaveConfig = true, ConfigFolder = "Orion"})

-- Player Tab --
local PlayerTab = Window:MakeTab({
    Name = "Player",
    Icon = "rbxassetid://4483345998",
    PremiumOnly = false
})

local PlayerSection = PlayerTab:AddSection({
    Name = "Player"
})

PlayerSection:AddSlider({
    Name = "Walkspeed",
    Min = 16,
    Max = 100,
    Default = 5,
    Color = Color3.fromRGB(255, 255, 255),
    Increment = 1,
    ValueName = "Walkspeed",
    Callback = function(Value)
        game.Players.LocalPlayer.Character.Humanoid.WalkSpeed = Value
    end
})
-- Player Tab End --

-- Settings Tab --
local SettingsTab = Window:MakeTab({
    Name = "Settings",
    Icon = "rbxassetid://4483345998",
    PremiumOnly = false
})

local SettingsSection = SettingsTab:AddSection({
    Name = "Settings"
})

SettingsSection:AddButton({
    Name = "Destroy UI",
    Callback = function()
        OrionLib:Destroy()
    end
})
-- Settings Tab End --

-- Highlight Players Tab --
local HighlightTab = Window:MakeTab({
    Name = "Highlight",
    Icon = "rbxassetid://4483345998",
    PremiumOnly = false
})

local HighlightSection = HighlightTab:AddSection({
    Name = "Highlight All Players"
})

local highlightedPlayers = {}

HighlightSection:AddButton({
    Name = "Highlight All",
    Callback = function()
        for _, player in ipairs(game.Players:GetPlayers()) do
            if player ~= game.Players.LocalPlayer then
                local character = player.Character
                if character then
                    local humanoid = character:FindFirstChildOfClass("Humanoid")
                    if humanoid then
                        -- Criar um efeito de destaque temporário
                        local highlightEffect = Instance.new("SelectionBox")
                        highlightEffect.Color3 = Color3.fromRGB(255, 255, 0)
                        highlightEffect.LineThickness = 0.05
                        highlightEffect.Adornee = character.PrimaryPart or character:FindFirstChild("HumanoidRootPart")
                        highlightEffect.Parent = character

                        table.insert(highlightedPlayers, {
                            Player = player,
                            HighlightEffect = highlightEffect
                        })
                    end
                end
            end
        end
    end
})

-- Desativa todos os efeitos de destaque
local function clearHighlightedPlayers()
    for _, info in ipairs(highlightedPlayers) do
        if info.HighlightEffect and info.HighlightEffect.Parent then
            info.HighlightEffect:Destroy()
        end
    end
    highlightedPlayers = {}
end

HighlightSection:AddButton({
    Name = "Clear Highlight",
    Callback = function()
        clearHighlightedPlayers()
    end
})
-- Highlight Players Tab End --

OrionLib:Init() -- Finaliza a inicialização da UI Lib
